# Example environment file for the Flutter mobile_app

# Base API URL for the backend (example)
API_BASE_URL=https://<backend-host>:3001

# App environment
APP_ENV=development

# Enable TV mode (Android TV 10-foot UI). Set to "true" to force TV UI on any device.
# If unset, TV mode defaults to true in release builds and false in debug/profile.
TV_MODE=false
