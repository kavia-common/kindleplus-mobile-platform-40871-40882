package com.example.mobile_app

import io.flutter.embedding.android.FlutterActivity

/**
 * PUBLIC_INTERFACE
 * TvLauncherActivity is a FlutterActivity that serves as the Leanback launcher entrypoint
 * for Android TV devices. It shares the same Dart entrypoint as the phone activity.
 */
class TvLauncherActivity : FlutterActivity()
